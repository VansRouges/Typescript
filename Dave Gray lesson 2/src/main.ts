let myName: string = 'Vance'
let meaningOfLife: number;
let isLoading: boolean;
let album: string | number;

myName = 'Michael'
meaningOfLife = 42
isLoading = true
album = 1984

const sum = (a:number, b:string) => {
    return a + b
}

let postId: string | number
let isActive: number | boolean

let re: RegExp = /w+/g
